# Decision log

Append-only, one line each — §46.4. Five weeks is long enough to forget why something was
chosen, and a re-litigated decision costs more than the original one did.

Newest last.

```
2026-08-29  initials/ stays local; the README describes it in prose rather than linking in.   — Jitam
2026-08-29  Rebase-only merges, branch deleted. main's log reads as a build log at task granularity. — Jitam
2026-08-29  Ladder order: 0.4 before 0.2. Without pyproject.toml, CI verifies nothing on models.py. — Jitam
2026-08-29  Default deps are pydantic only; statsmodels/sentence-transformers go behind extras CI does not install. — Jitam
2026-08-29  Gate command is `make gateN`, not `make gate N` — two-word make goals need a MAKECMDGOALS hack. — Jitam
2026-08-29  models.py and fixtures/ drafted solo while A and C await invites; both PRs stay open for their review before merge (§30 rule 1 deferred, not waived). — Jitam
2026-08-29  models.py carries three validators only: counted absence, coherent ranking, abstention-with-question. Everything else is a test. — Jitam
2026-08-29  D-3 settled: llm_cache/ is committed. §41.2 moved the row from Ignored to Committed. — Jitam
2026-08-29  No live provider at 0.5 — stub + replay only. AnthropicProvider lands at 1.5 with the first real prompt; shipping it earlier means unlinted, untyped code CI cannot install. — Jitam
2026-08-29  LLMProvider.complete is generic in T, not §18's literal `-> tuple[BaseModel, Usage]`. Same protocol, but call sites keep their types under disallow_untyped_defs. — Jitam
2026-08-29  CASEFILE_LLM_REPLAY defaults to on. Fail safe, not fail open: a missing var yields a CacheMiss, never a live call nobody intended to pay for. — Jitam
2026-08-29  Usage.cache_hit means the *provider's* prompt cache (§19), not the replay cache; replay never sets it. A cached_input_tokens field is the honest fix — treaty change, raise at the next sync. — Jitam
2026-08-30  §24's onset is 2026-03-12, not 2026-04-12, and the renewal lag U(18,30): §10's "21 and 23 days" and an April loss cannot both hold with an April onset. — Jitam
2026-08-30  The two treated accounts' renewal failure is injected, not sampled. §24 chooses the exogenous events; the model's job is to make them plausible (P(renew) = 0.041 / 0.082). — Jitam
2026-08-30  Per-entity RNG streams. One shared stream made every decision depend on draw order, so an unrelated change to ticket generation silently un-churned NORTHWIND. — Jitam
2026-08-30  `make check` runs the tools/ checks too. It did not, which is how a ground-truth violation reached CI at 0.7 with make check green — the divergence §41.3 says outranks everything. — Jitam
2026-08-30  P0 audit open item: fixtures/case_east_8pct.json attributes a share to driver_id "mix", which is not a registry driver. Needs the all-three sitting (§30 rule 1). — Jitam
2026-08-30  Ladder order: 2.7's remaining contracts and 2.8's scenarios D/E pulled ahead of 1.3/1.4. Same defect as 0.4-before-0.2 — 1.3 names D and E, 1.4 reads composition edges neither existed for. — Jitam
2026-08-30  STL is pinned to a flat seasonal profile (seasonal_deg=0), not the library default. Three cycles of history let a degree-one local fit interpolate a real drop as "a weak month"; a −₹2.6 Cr movement scored z=+0.66 before the fix. — Jitam
2026-08-30  persistence() uses hysteresis, not one threshold: the latest period must clear z_threshold, earlier ones only need to stay on the same side of expected. A step has exactly one period at 3σ by construction — requiring two would make §23's rule unsatisfiable for the case it exists to catch. — Jitam
2026-08-30  A regional monthly demand shock (σ 2% on metered usage) was added to the generator. Without it 46 accounts' usage lines averaged every wobble away and MAD collapsed toward zero, reporting six-figure z-scores. Regional rather than per-account, so K(2) stays ~0.91 rather than collapsing to ~0.5. — Jitam
2026-08-30  Completeness reads ingest punctuality (share of records inside the source's SLA), not §15's literal "28-day median by weekday". Billing lands one date a month; a row-count check also cannot tell a missing batch from two churned accounts, and would have closed scenario A as incomplete. — Jitam
2026-08-30  The artefact check reads both the current and prior period. A refund batch in one month makes the next look like a recovery of its own size; reading one period passed that case by luck. — Jitam
2026-08-30  PVM keeps an absent item's price on the side it's missing from, rather than reading it as 0. Otherwise a churned account reads as a price cut to zero and the loss smears across all three terms meaning nothing; §10 puts churn in volume, and this is what makes that true. — Jitam
2026-08-30  Cross-KPI attribution partitions the movement by what the CRM says happened to each account (renewed/launched/expanded), rather than summing ARR flows. Flows recorded this month can bill next month; summing them left a residual that was an artefact of the arithmetic, not the business. Measured residual: 0.00% against §23's 2% tolerance. — Jitam
2026-08-30  §35.2's pricing_change minor share (0.05–0.12) is not yet reproduced — measured −1.4%, through suppressed upsell alone, because PRICING_UPLIFT never moves the billed unit price PVM's price term reads. Flagged for 2.5/2.8, not fixed in 1.4: doing so moves the −8.47%/K(2)/all three scenario z-scores already measured. — Jitam
2026-08-30  Retrieval backend settled by measurement, not by §18's literal text: BM25 alone reaches recall@15 = 1.000 over every authored document, for every driver, queried with the hypothesis rationale. sentence-transformers ships behind an `embed` extra and stays opt-in. — Jitam
2026-08-30  BM25's top-k excludes documents scored -inf (no token overlap) rather than returning k regardless. Excluding on the *sign* of the score was tried first and is wrong: Okapi's IDF goes negative for a term in over half the corpus, which would have dropped the most on-topic documents in a footprint-scoped set. — Jitam
2026-08-30  Persona.region: str | None = None — the treaty's only P1 change. access.row.region.vp_sales: [own_region] has nothing to resolve `own` against without it. Optional and defaulted; no fixture moved. Landed without the all-three sitting (§30 rule 1) — nobody else was reachable — logged here and on the G1 agenda. — Jitam
2026-08-30  entitle.py folds an account's id and name to one canonical identifier before aliasing. Without it the same account got two different hashes depending on whether Stage 2's node or the ledger's prose named it first — a reader could count two accounts where there was one. — Jitam
2026-08-30  Money in entitle.py's payload is banded (band_1_5_cr), never rounded. A rounded number is still a number a reader treats as exact; only named money-bearing fields are banded, so a share, a z-score or a date is never mistaken for an amount. — Jitam
2026-08-30  ui/ ships React + Vite (§20) with a third CI job at 1.9, ahead of D-4's end-of-P2 deadline for that call — the fixture-driven screen needed to be verified, not assumed, as soon as it existed. — Jitam
2026-08-31  AnthropicProvider lands with hypothesise.py's S3 call — the first real prompt `provider_from_env` was waiting for since 0.5. Schema enforcement uses `messages.parse(output_format=...)` (§18's output_config.format), not tool-forcing. Untested against the live API — no key or network reaches it in this environment — so its tests verify the wrapper's own logic against a fabricated response, stated as a real gap in the module's own docstring rather than hidden. — Jitam
2026-08-31  anthropic joins default dependencies, not an extra: a few MB, and mypy type-checks the module that imports it regardless of whether CI ever calls it live. — Jitam
2026-08-31  Usage.input_tokens from AnthropicProvider sums input + cache_creation + cache_read — pricing.cost_inr has one flat rate, no cache discount, and summing overstates cost on a hit rather than understating it. The missing cached_input_tokens split is D-1's cache_hit gap again — still a treaty change, still not made solo. — Jitam
2026-08-31  The model-tier env var is CASEFILE_MODEL, per .env.example (committed at P0, e0a96bc) — not a name invented fresh here. Caught by rereading that file before merging, not by a test; nothing exercises .env.example against the code that reads it. — Jitam
2026-08-31  net_revenue.yaml's supply_delay had no probe_sql — expansion_arr.yaml and p1_resolution_time.yaml both point the same driver at incident_scan.sql; net_revenue's copy was missing the line. Fixed rather than left, since evidence.py now runs probe_sql for real and the gap would have silently skipped the probe on the headline KPI alone. — Jitam
2026-08-31  contract.py now checks probe_sql resolves to a real file — the same class of gap A1 already catches for lineage, one stage later. Existing PR15 test asserted the *absence* of this check on purpose ("not yet required to exist"); flipped to assert the opposite now that probes/ is real. — Jitam
2026-08-31  evidence.py puts an uncheckable item's driver in `supports` too, not neither list. models.py gives evidence exactly two linkage fields, and an uncheckable finding has to stay findable by Ledger.for_driver or Stage 6 cannot tell "no coverage" from "never probed" — the difference between Undetermined and a driver silently skipped. outcome carries the epistemic weight; supports here just means "about this driver." Caught by a test that assumed by_driver() would find every probed driver and didn't, on supply_delay's real uncheckable result. — Jitam
2026-08-31  _is_additive() now also requires no ratio denominator, not just SUM/COUNT aggregates. gross_renewal_rate is SUM/SUM — the aggregate check alone said "additive" and let a single ₹5 Cr renewal read as a 203-billion-percent share of a −0.17 ratio movement. Found while calibrating scenario B against a real dip already sitting in the committed corpus (2024-12), not against anything scenario B itself injects — this bug predates this PR and would have hit the first real gross_renewal_rate or nrr movement regardless. — Jitam
2026-08-31  decompose.py's per-dimension attribution (_nodes/_delta, recomputing the KPI value filtered to one key in each period) is correct for an additive SUM but not for a ratio-of-sums: a single-account slice of a ratio swings between 0 and 1 based on whether that account had any renewal due in each period at all, not on what actually happened to it — an account that churned with no renewal due in the comparison period showed delta=0 (invisible), while accounts that simply weren't up for renewal in one of the two periods showed the whole swing. Measured while calibrating scenario B on gross_renewal_rate; decompose() has apparently never been exercised against a real ratio movement before, since gross_renewal_rate's existing tests use a period where it does not move. Left unfixed and logged here rather than patched under time pressure — a correct ratio decomposition (rate/mix on numerator and denominator separately) is its own design, not a branch on the additive path. Scenario B moves to a different, additive KPI instead of waiting on it. — Jitam
2026-08-31  Scenario B lands on new_business_arr, not gross_renewal_rate — sidesteps the ratio-decomposition gap above entirely (SUM(opportunity.arr_value), no denominator) and carries no ongoing state (a lost new-logo deal doesn't churn an account's future billing the way a lost renewal would), which is what makes it safe to inject in any region/month without risking another scenario's already-measured figures. — Jitam
2026-08-31  A third pre-existing bug, found by the same calibration: verify.py's _scope() (shared by the artefact check's _largest_record and _slippage) never applied the contract's own `filters` — only the period and dimensions. For a KPI sourced from a table other row types also share (crm.opportunity holds renewal/expansion/new alike), the "largest record" scan picked up rows the metric's own formula never sums. A ₹5.58 Cr *renewal* row read as 1416% of new_business_arr's movement and refused a real case over a row that isn't part of new_business_arr at all. Fixed by mirroring metric.py's own filter-to-table matching in _scope(); verified against the real headline case (statistic dropped from 14.1575 to 0.6957 on the same query, the unrelated renewal no longer counted). — Jitam
2026-08-31  Scenario B's injected rows draw from their own RNG stream (`f"{world.seed}:scenario_b"`), not generator.py's shared rendering stream — and are excluded from _opportunity_notes' per-opportunity note-count loop by id prefix, not just by stream. Sharing the stream shifted every later draw in generation order, including _credit_notes' ordinary trickle, and silently moved East's already-measured April net_revenue figures with no causal link between the two events at all — the same failure class DECISIONS.md already logged for the NORTHWIND un-churn at P0, this time in generator.py's shared stream rather than scm.py's. Caught only by comparing against a byte-identical clean-main baseline, not by assumption. Verified: scenario A's total_delta and K(2) are now byte-identical to a clean checkout with scenario B present. — Jitam
2026-08-31  Scenario C modifies an existing ticket's resolved-at (a supply delay degrading P1 resolution time on APAC's footprint, Jan–Feb 2026) rather than adding rows — the same `rng.uniform(2, 96)` draw happens whether or not a ticket qualifies for the multiplier, so there is no extra draw to shift a later call's outcome the way scenario B's first attempt did. Verified anyway rather than assumed: East's total_delta and K(2) stayed byte-identical with scenario C present. p1_resolution_time.yaml's history_start already makes every case on it sparse by construction (§15), so the borrowed baseline and Likely ceiling needed no flag — only a real movement to open a case on. — Jitam
```
2026-08-31  decompose.py's ratio-attribution gap (logged above) is fixed: metric.py's value() now exposes numerator and denominator through a public term_total(), and _nodes computes each key's contribution as N_key,t/D_t - N_key,t-1/D_t-1 against a fixed, whole-scope (D_t, D_t-1) computed once in decompose() and threaded unchanged through every nesting depth — never re-sliced per key. That telescopes exactly (SUM partitions the numerator; the denominator is a shared constant), which is why it needed no rate/mix split after all: the earlier note's proposed design was heavier than the actual fix. Verified against gross_renewal_rate and nrr's real 2024-12 dip — every dimension's nodes sum to total_delta to float precision — and by two mutation tests (reverting to the old per-key value() call; swapping D_t/D_t-1), both caught by the new sum-invariant tests in test_decompose.py. — Jitam
2026-08-31  EvidenceItem gains quote: str | None = None — ladder step 2.3's own verify text ("every claim carries doc_id and a quoted span") had no field to land in. doc_id needed none: source.record_id already carries it, matching 4a's own convention. Validated: required exactly when method == "llm_extraction" and outcome == "found" — the absence/uncheckable placeholders 4c emits point at a driver's whole document set, not one document, so they carry no single quote. Additive, optional, landed solo per §30 rule 1 (nobody else reachable, same as Persona.region) — flagged for the G2 agenda. fixtures/case_east_8pct.json's two pre-existing llm_extraction items (ev-004, ev-007, hand-written before this field existed) needed a quote each to keep validating; added by hand, consistent with the rest of that fixture's illustrative, not-generator-derived content. — Jitam
2026-08-31  evidence.py 4c calls the model once per driver, not once per case — each driver's Funnel is scoped to its own evidence_sources/max_lag_days, so unlike 4a's single-registry hypothesise.py call there is no one combined document set to extract from in one shot. Returns list[Usage], one per driver actually called, rather than summing into a single figure — that aggregation is telemetry's job (P3), not this stage's. — Jitam
2026-08-31  4c never lets the model self-report a confidence number for an extracted claim — same rule hypothesise.py's system prompt already enforces ("never write a number... those are computed later"). strength is a flat code-side constant instead: 0.6 for a verified quote (weaker than 4a's measured facts), 0.5 for "documents were read, nothing extracted." Every claim's doc_id and quote are still verified against the retrieved Funnel in code before becoming an EvidenceItem — schema-forced guarantees the response's shape, not its content, and the guardrail is what checks the content, mirroring hypothesise.py's own philosophy. — Jitam
2026-08-31  4c reuses 4a's three-outcome discipline exactly: zero documents in the funnel (no coverage at all) is uncheckable; documents retrieved and read but nothing extracted is checked_absent, denominator = documents read; a surviving claim is found. A driver whose evidence_sources are entirely structured (seasonality's historical_series, which names no table retrieval can read) is skipped outright — no item, no call — the same reason 4a skips a driver with no probe_sql. Outcome is a property of what the text says, never of which corpus role (signal/misdirection/noise) a human author tagged it — the pipeline cannot see that tag and would not be allowed to use it if it could. — Jitam
2026-08-31  challenge.py's Control test is computed once per case, not once per hypothesis — every driver challenged in the same call sees the identical DiD/placebo-rank result. §15's own formula and every worked example describe Control as treated-footprint-accounts-vs-matched-peers with no per-hypothesis construction, and the sealed answer sheet's own killed_by field never names control for either of scenario A's decoys (only locality, and locality_and_timing) — direct evidence the discriminating tests are Timing/Locality, not Control. A per-hypothesis treated/control construction was considered and rejected: with the true cause's effect this large (two full account churns), it dominates any peer comparison regardless of which population the controls are drawn from, so a hypothesis-specific Control would not actually discriminate — it would just look like Locality wearing a different name. — Jitam
2026-08-31  Timing's τ_effect, per footprint account, is that account's own crm.renewal.closed_date inside the window — the one discrete "the effect happened here" event a revenue movement in this project traces back to. An account with no renewal in the window contributes no pair rather than erroring; if none do, Timing is inconclusive. This is honest about its own scope: it is tuned to renewal-outcome-shaped movements (net_revenue's real scenarios), not proven general to every KPI shape. Verified against the real numbers, not assumed: onset 2026-03-12, closed_dates 2026-04-02/04, giving exactly the 21/23-day lags §10's worked example quotes. — Jitam
2026-08-31  Locality's cause footprint is deliberately computed wider than the KPI's own footprint — region-scoped peers for the ticket-spike driver, segment-scoped peers for the pricing driver, region-scoped peers for the competitor driver — dispatched by probe_sql filename stem, the same registry pattern evidence.py's _INTERPRETERS already uses. Verified against real data for all three of scenario A's hypotheses: J=1.0 (2 of 2 East accounts spiked — the true cause), J=0.049 (2 of 41 enterprise accounts actually declined — pricing, refuted), J=0.0 (footprint is East, competitor's news is APAC-only — refuted). All three match §10's worked example exactly. — Jitam
2026-08-31  A real bug caught before merge: `TestResult.outcome` (pass/refute/inconclusive) and `EvidenceItem.outcome` (found/checked_absent/uncheckable) are two different vocabularies, and the first draft of `_evidence()` compared `TestResult.outcome == "uncheckable"` — a literal that value never takes, so `coverage` was silently never set and every inconclusive item crashed EvidenceItem's own validator on the very first real run. Fixed by mapping explicitly (`inconclusive -> uncheckable`) into a named local rather than comparing across vocabularies inline; a mutation test confirms reverting the fix is caught by two independent tests. — Jitam
2026-08-31  challenge.py's tests live at tests/test_challenge.py, not tests/engine/test_challenge.py as docs/09-build-protocol.md's ladder line states — every other engine/ module's tests (decompose, evidence, hypothesise, entitle) already live flat in tests/, and tests/engine/ has never existed. Followed the real, established convention over the stale doc reference; a doc correction for this belongs with the rest of the G2 gate-docs pass. — Jitam
2026-08-31  A real bug found while building adjudicate.py, in challenge.py: _lost_reason_footprint returned set() (an empty cause footprint) whenever no competitor-tagged news existed at all, which Locality then scored as J=0.0 — refuted. That is right when a real, dense population was checked and came back empty (price_book's "always has full history" case), but wrong when there was nothing to check in the first place: scenario B's true cause is a competitor pressure the sources genuinely cannot see, and refuting it on Locality would have been exactly the "confidently wrong" failure this whole project exists to prevent. Fixed to return None (inconclusive) instead, matching evidence.py's own uncheckable finding on the same mechanism. Caught by measuring scenario B end to end, not assumed — scenario A's real news item (competitor-promotion-apac) means this fix changes nothing there. — Jitam
2026-08-31  adjudicate.py's per-hypothesis tier rule makes Confirmed structurally unreachable below n=5 accounts without a single special case for Dose: Confirmed requires >=2 tests passing (Timing or Locality among them), Control passing, and nothing inconclusive anywhere — and Dose cannot itself return "pass" below stats/correlation.py's own MIN_PAIRS=5, so at n=2 it is always inconclusive, which the same "nothing inconclusive" clause already disqualifies. No dose-specific code exists in adjudicate.py at all; the invariant falls out of the general rule. Verified against real scenario A: Dose inconclusive at n=2 there, case lands on Likely, matching the ladder's own verify text exactly. — Jitam
2026-08-31  Attribution.status distinguishes eliminated from minor by whether the driver has independently found supporting evidence anywhere in the ledger, not by which test refuted it: a driver refuted as the primary explanation but with a real, separately-found effect (pricing_change's genuine price increase, found by evidence.py's 4a probe) is minor; a driver with no found evidence anywhere, refuted or not, is eliminated when a test names the reason, else unresolved. This last case is scenario B's true cause exactly — competitor_offer refutes nothing (nothing was checkable) and supports nothing (only an uncheckable item exists) — so it is unresolved, never eliminated, which is the whole point: an elimination the case cannot actually trace would be an opinion, and unresolved says the honest thing instead. Control's own "found" item is excluded from this support check — it is computed once per case and shared across every hypothesis (see the earlier entry on why), so it would otherwise count as independent support for every driver equally and erase the distinction entirely. Caught by measuring scenario A end to end: without the exclusion, supply_delay and competitor_offer both read "minor" off Control's shared pass. — Jitam
2026-08-31  Attribution.share is left None for every refuted or unresolved driver except one narrow, principled case: a driver whose entire mechanism is a price change (its probe_sql is price_delta) gets PVM.price / total_delta, since §23's PVM already isolates that component for free on any KPI billed at a quantity and a price, and it is a real, separately-measured number rather than a re-slice of the account-level movement the primary already explains. No general "minor share" formula was attempted beyond this — the hand-authored fixtures/case_east_8pct.json's fuller attribution (including a "mix" entry with driver_id "mix", which is not a registry driver, an open item already logged at P0 and still unresolved per §30 rule 1) is not what adjudicate.py's real output is graded against; tests/test_fixtures.py validates that static, illustrative file directly and calls none of this module's code. Reproducing "mix" would need the same all-three sitting that entry is still waiting on, so this module deliberately does not attempt it. — Jitam
2026-08-31  Confidence weights for Case.priority: confirmed=1.0, likely=0.8 (the only one stated anywhere — 16.8M/21M footprint delta in the golden fixture, exact), contested=0.6, undetermined=0.4. No table for the other three exists in any doc; chosen monotone with confidence and never zero, so an unexplained movement never falls off the bottom of a priority-ordered case list — internal implementation constants, not a schema change, so landed solo like evidence.py's SPIKE_RATIO and DOCUMENT_STRENGTH before it. — Jitam
2026-08-31  Scenario G (Contested) has no generator support yet — ladder step 2.8 (scenario injection) comes after 2.5 (adjudicate.py) in the build order, and §47.3 lists G as cheap to cut last if time runs short. adjudicate.py's Contested path (>=2 hypotheses surviving to Likely or better in the same case) is built and tested against a hand-built TestMatrix pair instead — the same approach test_evidence.py already uses for a real-data gap in _lost_reason_scan's denominator distinction. Will be re-verified against a real run once 2.8 lands. — Jitam
2026-08-31  recommend.py only fires for a `primary` attribution — Verdict's own validator ties `primary` 1:1 to confidence in (confirmed, likely), so this is exactly "no recommendation below Likely." Contested and Undetermined already have their own actionable next step, Stage 6's discriminating question; naming a specific lever against a driver the case itself says it cannot separate from a rival would be the overclaiming §9 exists to prevent. Verified against real scenario B (Undetermined): recommend() returns None. — Jitam
2026-08-31  expected_impact is |tree.total_delta| × lever.save_rate, not |tree.footprint.delta| — settled by exact arithmetic against fixtures/case_east_8pct.json's own numbers, not prose: [18000000.0, 24000000.0] only reconstructs as 24000000.0 (total_delta) × [0.75, 1.00]; footprint.delta (21000000.0) gives a different, non-matching pair. Consistent with adjudicate.py's own priority formula, which already reads total_delta as "the value at stake" case-wide; OpenQuestion.value_at_stake stays on footprint.delta, the narrower, already-established convention for that field. — Jitam
2026-08-31  monitoring reuses contract.refresh.cadence (how often this KPI's data moves at all) and contract.materiality.min_persistence (Stage 1's own "how many periods to call a movement real," reused here as "how many periods to wait before escalating") — both genuine contract lookups, zero invented fields. This does not reproduce fixtures/case_east_8pct.json's monitoring text verbatim ("renewal_rate", "weekly", owner "vp_sales"): the fixture names a downstream sub-KPI net_revenue's own composition edges reach, and no field anywhere records which sub-KPI a driver's lever should be watched through, so inventing that mapping would be exactly the kind of schema-adjacent guess §30 rule 1 exists to stop. Same divergence class as adjudicate.py's non-reproduction of the fixture's "mix" attribution entry — the fixture is illustrative, not the grading target. — Jitam
2026-08-31  Action text is a 4-entry dict keyed by lever.action, one sentence per lever — verified closed over every current contract (`grep -oh "action: [a-z_]*" contracts/*.yaml | sort -u` returns exactly these 4). A lever.action outside that set raises RecommendError rather than falling back to generic text: Lever.action is a plain str, not a closed Literal, so a future contract could add a fifth lever with nobody having agreed on its sentence yet — that is a registry addition, not something to paper over silently. A driver with no lever at all (Driver.lever is Lever | None "where nothing is controllable," not only seasonality's case) returns None the same way a non-primary attribution does. — Jitam
2026-08-31  Scenario G attempted and deferred: measured against the real corpus (segment sizes: enterprise 41, mid_market 43, smb 36, none region-scoped) rather than assumed. challenge.py's price_delta Locality searches segment-wide with no region filter (already validated against scenario A's real J=0.049 refute) — for any 5-account footprint that forces J <= 5/36 ~= 0.14 whether or not a price change is injected in-window (injecting nothing scores J=0.0, injecting one scores J<=0.14; both are below LOCALITY_REFUTE=0.2), and adjudicate.py's _tier eliminates a hypothesis outright on a single refute regardless of what else passes — so pricing_change cannot reach Likely on a literal 5-account West footprint without either enlarging the footprint past ~10 accounts or narrowing Locality's search scope in already-merged code. Separately, Timing's effect-date is hardcoded to crm.renewal.closed_date (already logged as scoped to renewal-shaped movements); expansion_arr's real effect event is an opportunity close, so Timing has nothing to key off there without a coincidental or injected renewal. Per §47.3 ("cheap; cut last") and since adjudicate.py's Contested path is already built and tested against a hand-built TestMatrix fixture (tests/test_adjudicate.py), deferred rather than forced — the two paths that would make it work (a larger, differently-shaped footprint, or extending already-merged challenge.py logic with a re-verification of scenario A's real numbers) are both real options, left for later if time allows. — Jitam
2026-08-31  Ladder step 2.9: CaseList (Screen 1) and Ledger (Screen 3) added, with App.tsx now navigating list -> detail -> back instead of rendering the one fixture case directly. CaseList sorts by case.priority descending — a required field on every Case, closed or not, so a case closed at Verify (!verification.passed, zero model calls) still sorts rather than needing an invented priority convention. Ledger is a sibling of CaseFile, not folded into its six blocks — §11 names it as its own screen, and putting it inside <main data-testid="case-file"> would make CaseFile's own "exactly six blocks" test start counting it. WhatWeTested's per-test cell now links each evidence_id to the ledger via a plain in-page anchor (href="#ev-...") — no router added, since a same-page anchor is the whole mechanism §11's "click any claim" asks for. — Jitam
2026-08-31  Only one real Case exists anywhere in this repository (fixtures/case_east_8pct.json) — the list ships with one row rather than several invented ones. CaseList and Ledger are both generic over Case[]/EvidenceItem[] and are proven against synthetic priority/verdict variants in their own tests (the same spread-and-override pattern CaseFile.test.tsx's own "degraded case" fixture already uses), not against fabricated production fixtures — this project has been careful all session to keep "real, measured" and "illustrative, hand-authored" separate, and a demo case invented purely to make the list look fuller would blur that line for no ladder-required reason. — Jitam
2026-08-31  D-4 settled: React (not Streamlit). Both of §11's Screens 1 and 2 render by G2 — CaseFile (Screen 2) since 1.9, CaseList (Screen 1) as of this same gate — with 27 passing tests and a clean tsc+vite build, which is exactly D-4's own measured trigger for keeping React over falling back to Streamlit. — Jitam
2026-08-31  Scenario F (§25, "Support Lead opens case A") verified against the real pipeline, not just the hand-written fixture entitle.py's existing tests already used. Correction to an earlier premise: entitle.py is ladder step 1.7 (P1, long merged, not P3) — the scenario-G AskUserQuestion this same day wrongly cited it as unbuilt when reasoning about scenario F's dependency; it was only narrate.py (S8b, genuinely P3) that F's full presentation eventually needs, and this verification does not touch that. A real finding while building it: the domain filter (segment) withholds every enterprise account in the whole decomposition tree, not only the two carrying scenario A's own movement — East carries 16 enterprise accounts, not 2 — because §14.1's access.domain rule is a statement about what a role may see, not about one case's footprint. The test computes its expected count from the real accounts table rather than assuming 2, which is what caught the wrong assumption before it shipped. — Jitam
2026-08-31  orchestrator.py (ladder 3.1) wires S1 through S7 into one run_case(contract, period, dimensions, con, provider) -> Case, with S0 already done at make data time and S8a/8b deliberately excluded — those run per persona over a finished Case, a different question from "what is the case." A case that fails Verify returns immediately (no decomposition, no model calls, priority=0.0) rather than raising; AdjudicateError's "nothing was challenged" is left unguarded since every current contract has at least one probed driver, making it unreachable, the same reasoning already applied to adjudicate.py's own confirmed-unreachable invariant. Every stage's wall time and every model call's Usage folds into one Telemetry object, assembled here rather than aggregated elsewhere, since this is the one place all of it already converges. — Jitam
2026-08-31  Trigger.delta/delta_relative are computed independently of decompose() (a small calendar-aware previous-period lookup mirroring decompose.py's own private _window(), not reusing it — Trigger has to exist even for a case that closes at Verify, before decompose ever runs). delta is overwritten with tree.total_delta once decompose succeeds (exact, already-computed); delta_relative is not recomputed and stays whatever the independent lookup found — verified against the real number both ways, since a mutation swapping which period counts as "previous" broke delta_relative (0.0 instead of the real −8.47%) without any other test noticing, because delta's own overwrite masked it. Caught by mutation testing before merge, not left as a gap. — Jitam
2026-08-31  make demo runs orchestrator.py against §10's own worked example (net_revenue, East, 2026-04) — the one case named on stage, not a general movement scanner (out of this ladder step's scope). It cannot complete in this environment: llm_cache/ has no recorded response for this exact prompt and no API key reaches the network here, so it fails loudly with an explicit message naming both ways to fix it (record one with a real key, or supply an already-cached run) rather than hanging or succeeding vacuously. Verified the failure path itself is clean — same class of environment gap already logged for AnthropicProvider's own live-API testing. — Jitam
2026-08-31  Makefile's demo target landed solo — a shared, all-three-sign-off file per CODEOWNERS, same as models.py. depends on `data` so the warehouse can never be stale; teammates still unreachable, flagged here per the established §30 rule 1 pattern rather than waiting. — Jitam
2026-08-31  data/ground_truth.json was stale — committed before scenarios B and C existed in generator.py, so it only carried A/D/E. Found as a side effect of make demo's own `data` prerequisite regenerating it fresh; refreshed and committed (purely additive, 80 lines, nothing removed) to restore "same seed, same bytes" between the generator and its own committed snapshot. — Jitam
2026-08-31  narrate.py (ladder 3.2, S8b, LLM #3) keeps "the model never produces a number" a checked fact rather than a hope by never letting it write one at all: it is handed a fixed menu of named {token} strings (already formatted, already banded if this persona's amount_net is restricted) and told to write prose around them, never inside them. The guardrail strips every recognised {token} and [ev-xxx] citation from a section and rejects it if any digit survives, or if it used an unrecognised token, an invented citation, or was blank — falling back to a fully-templated, still-correct sentence built from the same tokens, per section, not all-or-nothing. This avoids the alternative §17 explicitly rules out ("nothing downstream ever parses free text") — extracting numbers back out of prose to verify them would be exactly that. — Jitam
2026-08-31  narrate.py bands amounts itself, reusing entitle.py's own band() — not something entitle.py could do for it. entitle.py's own docstring already explains why: Case's numeric fields (trigger.delta, priority, expected_impact) stay exact floats in the typed object even after entitlement ("a float cannot hold '₹1-5 Cr'"), and only the separate JSON payload gets banded. Since narration reads the typed Case, not the payload, it is the one place those figures actually reach text a restricted persona might read — so it re-checks contract.access.column["amount_net"] itself rather than trusting the Case object to already be safe. Verified against real, generated scenario A data for both an unrestricted persona (cfo, exact ₹) and support_lead (banded), mutation-tested. — Jitam
2026-08-31  Three real bugs caught before merge, all by tests failing on the first run: (1) primary_share used the same one-decimal _percent() as delta_pct, producing "87.5%" where the UI's own share() convention (format.ts) rounds a driver's share to a whole percent — added a separate _share() rather than overloading _percent with a second meaning. (2) The guardrail's subset checks (used_tokens <= tokens.keys(), used_citations <= ledger_ids) are vacuously true for an empty string, so a blank section from the model passed straight through instead of falling back — added an explicit text.strip() check first. (3) _prompt() never received the persona at all in the first draft, so the model was never told whose page it was writing — caught by rereading the module before writing tests, not by a test itself. — Jitam
2026-08-31  narrate.py does not mechanically check "every sentence cites a ledger id" — only that every citation actually used is real, and (for the model's own prose, not the deterministic fallback) that the section is not made entirely of an unearned digit. Enforcing the per-sentence rule would need real sentence segmentation to avoid false positives on abbreviations and citation-adjacent punctuation; stated as a simplification in the module's own docstring rather than silently narrower than the spec text. — Jitam
2026-08-31  Ladder step 3.3, UI persona switcher: built fixture-first, not against a live pipeline, matching Track C's own original implementation order (persona switcher, step 7, comes before orchestrator.py, step 8). tools/build_entitled_fixtures.py runs entitle() once per persona over the golden case_east_8pct.json and commits the four results as fixtures/case_east_8pct_entitled.json — entitle() is Stage 8a, DET, no model call, so this needed no llm_cache/ entry the way a narrate.py-backed version would have. ACME/NORTHWIND's AccountFacts are hand-entered in the script rather than looked up from the warehouse, since case_east_8pct.json is itself hand-written and this script's job is a static, reproducible transform of one fixture into another. — Jitam
2026-08-31  The entitled payload bands a restricted amount in place, at the same JSON path a typed Case carries a float — entitle.py's own docstring already explains why (a float cannot hold "₹1-5 Cr"). PersonaSwitcher.tsx therefore reads a separate, narrower EntitledPayload type with those specific fields widened to Moneyish (number | string), rather than trying to make the existing six-block CaseFile components generically handle both typed and banded data — a much larger change for a screen whose own verify text ("masked names and banded ₹, with the redaction stated") does not ask for it. — Jitam
2026-08-31  Ladder step 3.4, telemetry aggregation, verified against a real run rather than only the golden fixture's own hand-typed numbers (test_fixtures.py's total_cost_inr < 10 / total_latency_s < 10 checks, and Telemetry's own arithmetic properties, already existed from P0 — this closes the "real, not just typed-in" gap the rest of this session's work has repeatedly closed the same way). Measured directly: the six deterministic stages of a real scenario A run complete in well under a second (StubProvider), and exactly two model calls happen (s3, s4c) — not four, since only integration_delay's funnel yields a document worth extracting from in this footprint and window, matching evidence.py 4c's own per-driver call discipline. The two real calls' own cost and latency cannot be measured live here (no API key/network, same class of gap already logged for make demo and AnthropicProvider) — the golden fixture's own recorded s3+s4c figures (₹6.7, 4.5s) stand in as the best available evidence, and combined with the real deterministic measurement, the full run's estimated total (well under 2s deterministic + 4.5s) sits comfortably inside the §19 budget with real headroom. — Jitam
2026-08-31  Ladder step 3.5, UI telemetry panel: a sibling of CaseFile, same treatment Ledger (Screen 3) already got, for the same reason — §11 names it its own screen, and nesting it inside <main data-testid="case-file"> would make CaseFile's own "exactly six blocks" test start counting it. Its three totals (cost, latency, LLM/non-LLM split) are plain addition over telemetry.calls/stages in TypeScript, mirroring Telemetry's own Python properties exactly — those are computed, not stored fields, on the Python side too, so this is not the "no arithmetic" rule format.ts states (which governs a business figure a deterministic stage upstream already computed — Δ, share, PVM — not the receipt this screen exists to show). Caught one real bug before it shipped: the first draft reused crore() for cost_inr, which divides by ten million and would have rendered a real ₹6.7 call cost as "₹0.0 Cr" — cost_inr is rupees, not crores; fixed with its own small formatter. — Jitam
2026-08-31  Ladder step 3.6, the cadence upgrade path: run_case, called again against a real warehouse with a source's watermark moved back (the same technique test_verify.py's own staleness test already uses, more faithful than moving as_of — it is the batch that is late, not the clock), correctly produces a provisional case with confidence_ceiling=likely, and the same case re-run against the real, unmutated watermark is no longer provisional and carries no ceiling. The ceiling actually capping a verdict's confidence is already proven separately (test_adjudicate.py's synthetic Confirmed-to-Likely test — no real generated scenario reaches Confirmed to be capped, by the same n>=5 Dose invariant already logged) and the staleness/ceiling mechanism itself is already proven in test_verify.py; what this closes is the remaining, orchestrator-level claim that run_case is the re-run — the same function, on the same real case, not a special path. — Jitam
2026-08-31  Real bug caught while building the above: test_orchestrator.py, test_narrate.py and test_telemetry.py — all built this session, all genuinely P3 ladder scope (3.1/3.2/3.4) — were marked pytest.mark.gate2, not gate3, copied from nearby P2 test files without checking the ladder phase had moved on. make gate3 returned zero tests until this fix; retagged all three plus this file's own tests, verified gate2 (75) and gate3 (23) both now match what each phase actually shipped. — Jitam
2026-08-31  A real gap, found by looking at the running UI rather than the code: App.tsx rendered exactly one case, and that case was fixtures/case_east_8pct.json — hand-typed at P0, before the engine existed, not orchestrator.run_case()'s output. A demo viewer has no way to tell "the pipeline computed this" from "someone typed this to match §10" apart, and for the one case on screen the honest answer was the second one. Fixed by tools/build_real_case_fixtures.py, which regenerates the corpus from the committed seed and calls the real run_case() — the same function test_orchestrator.py and make demo call, with StubProvider standing in for the missing live key — for scenarios A, B and D (§25), writing fixtures/case_real_scenario_{a,b,d}.json; App.tsx now renders those three, sorted by the same real priority field. case_east_8pct.json and its entitled sibling are untouched — test_fixtures.py, test_narrate.py and the persona-switcher/ledger/telemetry UI tests all still key off them directly, by file, not through App.tsx. tools/build_real_entitled_fixture.py is the same transform tools/build_entitled_fixtures.py already does, pointed at the new real scenario-A case instead. — Jitam
2026-08-31  One real, honest narrative difference from the golden fixture, worth stating rather than hiding: in the real scenario A run, S4c extraction's guardrail correctly rejects StubProvider's fabricated quote (it is not a verbatim substring of any real retrieved document) and the item lands as checked_absent instead of found — proof the guardrail actually guards, not a defect. pricing_change still lands as minor, not eliminated, because its found support comes from real S4a SQL probes (six price_book rows, method="sql"), independent of extraction entirely — so the one worked-example detail this run cannot reproduce without a live key is a *second*, LLM-sourced piece of supporting evidence for pricing_change, not its minor status itself. The persona switcher is gated to render only on the case whose id matches the entitled fixture's source case (scenario A) — App.tsx no longer has an implicit single-case assumption to rely on, and showing scenario A's entitled view under a different case's page would be exactly the kind of silent mismatch this fix exists to remove. — Jitam
2026-08-31  Real, live-looking secret keys (correct vendor prefixes/shapes for all three) were found sitting in the locally-modified, git-tracked .env.example while adding the Gemini/Groq toggle below — flagged to the user before any further edit rather than silently perpetuated; nothing had reached origin/main (checked: HEAD's own copy still had ANTHROPIC_API_KEY empty). Not committed, not written back by this session. — Jitam
2026-08-31  LLM_PROVIDER (anthropic|google|groq, default anthropic) and LLM_MODEL (generic, optional, provider-scoped) added to provider_from_env(). CASEFILE_MODEL keeps working standalone for Anthropic-only use — LLM_MODEL only takes precedence when both are set — so every call site written before this existed is unaffected. google-genai and groq land behind a new `providers` extra, not default deps, mirroring anthropic's own dev-comment reasoning in reverse: anthropic is the one CI actually type-checks a live call against, so it stays a default dependency; the other two are genuinely optional and would be paid for on every push for two vendors most runs will not select. Both new provider classes import their SDK lazily inside __init__ (not this module's top level) with a clear ImportError naming the extra when it's missing — the exact mechanics casefile.retrieval.rank.MiniLMRanker already uses for sentence-transformers/embed, copied rather than reinvented. Consequence: neither provider's own test file can exercise complete()'s wire-adjacent logic the way test_anthropic_provider.py does (that needs a real client to monkeypatch, and the package genuinely isn't installed here) — only the data invariants (every priced class has a live id) and the guard (constructing without the extra raises ImportError) are verified; stated as a real gap in each module's own docstring, the same discipline anthropic_provider.py already holds itself to for its own live-API gap. — Jitam
2026-08-31  Gemini and Groq model ids and prices are sourced, not recalled from training: ai.google.dev/gemini-api/docs/pricing and console.groq.com/docs/models, both fetched today. Only two Groq models had a confirmed public per-token price on that page (openai/gpt-oss-120b, openai/gpt-oss-20b) — no other Groq model was added, matching cost_inr's own existing rule that an unpriced class raises rather than silently reporting ₹0. gemini-flash (3.7 Flash) is discount-priced through 2026-12-31 and doubles on 2027-01-01 — commented at both PRICES's entry and gemini_provider.MODEL_IDS's, so the number doesn't go quietly stale. — Jitam
2026-08-31  Groq's strict json_schema mode needs additionalProperties: false on every object in the schema, which Pydantic's model_json_schema() does not set by default — groq_provider._strict() walks the schema (including $defs, where a nested BaseModel like Hypothesis or ExtractedClaim lands, not inline) and adds it. Neither Gemini's response_json_schema nor Groq's strict mode has been checked live against this project's own nested schemas (HypothesiseResponse, ExtractionResponse) — only Groq's docs' own flat example and NarrationResponse (which is flat) are confirmed by documentation; stated as an open question in both provider modules' docstrings rather than assumed solved. — Jitam
2026-08-31  The two providers' own "untested against a live endpoint" gap, closed for real: pip install -e ".[providers]" plus a real key for all three vendors, run against actual project schemas (HypothesiseResponse, ExtractionResponse, NarrationResponse), not toy ones. Gemini: correct, zero transformation needed, confirmed against the nested schema directly — but two of the three original model ids were simply wrong. gemini-2.5-pro and gemini-2.5-flash-lite both 404 ("no longer available to new users"); Google's own error names the replacement, confirmed live in turn: gemini-3.1-pro-preview (429 on this free-tier key — a quota limit, not a 404, so the id itself is real) and gemini-3.5-flash-lite (clean success). gemini-3.7-flash, the "flash" default, hit repeated 503s under real load this session — kept as-is (a real, current, non-404 model; 503 is Google's own "high demand" language, not evidence of a wrong id) rather than swapped for the merely-more-convenient gemini-3.5-flash that happened to work at the same moment. Pricing updated to match the real ids: gemini-pro at 3.1 Pro Preview's own $2/$12 (≤200k-token tier — this project's own ~14.5k-token cases never reach the $4/$18 tier), gemini-lite at 3.5 Flash-Lite's $0.30/$2.50 (unchanged number, different underlying id). — Jitam
2026-08-31  Groq: a real bug, caught only by the live call, not the docs. _strict()'s first version added additionalProperties: false alone; a live HypothesiseResponse call (nests Signature, four Optional[str] = None fields) came back 400 from Groq's own schema validator, naming the exact missing keys — required must list every property, including nullable ones with a default, because strict mode has no "optional but present" concept. Fixed by having _strict() also set required = list(properties.keys()) on every object, $defs included; re-verified live against both of this project's real nested schemas (HypothesiseResponse via groq-120b, ExtractionResponse via groq-120b) and the flat one (NarrationResponse via groq-20b) — all three now round-trip correctly. One separate, non-bug finding along the way: groq-20b (not -120b) produced an empty {} for expected_signature instead of the four required nulls on one run — a smaller model's instruction-following gap under strict mode's stricter contract, not a schema defect; DEFAULT_MODEL_CLASS was already groq-120b before this was found, so the default was never affected, but it's a live reason to keep -120b as the default rather than treat -20b as a drop-in cheaper substitute for the nested calls (S3, S4c) specifically. — Jitam
2026-09-02  Ladder step 4.1, feedback.py S9 (the ADA-integration plan's ADA-1, docs/ada-integration-plan.md): three pure functions per the Stage 9 table in §02 architecture — apply()/driver_prior() track a learned prior per (kpi, driver_id, segment), gathering_depth() cuts to probe-only once a driver is two uncontested wrong_driver marks in the hole (one correct_driver cancels one wrong), reorder() re-sorts a hypothesis list by that prior without adding, removing, or re-testing anything (S3's own priority field stays presentation-only, per its docstring), adjusted_materiality() relaxes S1's dual gate per not_material mark for that KPI (capped at 10 marks: +10 points relative, ×1.05¹⁰ absolute), and promote() adds a missed_cause mark's driver to a *copy* of the contract's registry. FeedbackMark/LearningState live in feedback.py itself, not models.py — the same precedent HypothesiseResponse already set in hypothesise.py, since nothing here crosses the A/B/C track boundary models.py's own docstring reserves for. All five functions verified pure by test (state/contract passed in is provably unchanged after every call) and mutation-tested by hand before this entry: flipping the probe_only boundary (<= to <), the reorder sort's sign, the promote duplicate-id guard, the correct/wrong step signs, and the not_material cap were each introduced one at a time and each was caught by a failing test, then reverted. Deliberately not wired into orchestrator.py this pass — the ladder line for this step reads "feedback.py S9 — priors, threshold tuning, gap promotion", and every other stage's own module was itself built and unit-verified before its own orchestrator wiring landed as a separate, later change; wiring LearningState into hypothesise.py's/evidence.py's live call sites is real remaining work, not assumed done here. — Jitam
2026-09-02  ADA-2 (docs/ada-integration-plan.md): tools/calibrate_materiality.py measures S1's real false-alarm rate the way ADA's own tools/calibrate_anomalies.py measures its anomaly band — simulated stable series through the real assess(), not the domain generator. The domain generator turned out to be the wrong instrument for this: scenario A/B/C/D's footprints (TREATED accounts, refund_batch_account, scenario_b/c_accounts) are selected by fixed name/region/sort-order logic in data/scm.py, not by the per-seed rng, so re-seeding generate() would not have produced genuinely null draws — every seed still carries the same structurally-guaranteed movements, just with different noise around them. Switched to simulating directly against assess() instead, each contract's synthetic level set to absolute÷relative (the scale at which its own two thresholds coincide, derived rather than guessed). Measured, 3,000 trials/contract: expansion_arr 0.07%, gross_renewal_rate 0.27%, net_revenue 0.43%, new_business_arr 0.00%, nrr 0.43%, p1_resolution_time 0.00% — all comfortably under ADA's own 5% reference target, dominated by min_persistence>=2 (two consecutive 3-sigma-crossing periods under independent noise is rare before relative/absolute are even checked). Table lives in docs/06-quality.md §35.6; tests/test_materiality.py::test_the_gate_rarely_fires_on_genuinely_stable_data is a fast 500-trial regression guard on the same property, bound at 5%. — Jitam
2026-09-02  ADA-3 (docs/ada-integration-plan.md): tests/test_no_raw_data_in_prompts.py turns §17's "none of them produce a number" from a doc claim into a test, the same way evidence.py's own _guardrail already turns "a quote must be real" into one. Sentinel is a real amount_net invoice-line figure (₹31,66,475.00) for scenario A's own ACC-0001, queried from the real generated warehouse against the real case_real_scenario_a.json fixture — not a synthetic magic value, since the real number was already distinctive enough. Checked against the real Prompt.system/.user text each of hypothesise._prompt (S3), evidence._prompt (S4c), and narrate._prompt (S8b) actually build. All three passed clean on the first real run; the test itself was mutation-tested before being trusted — a temporary edit interpolating the sentinel into evidence._prompt's own user string was caught immediately by the new test, then reverted, confirming the guard actually guards rather than passing vacuously. — Jitam
2026-09-02  ADA-4 (docs/ada-integration-plan.md): narrate.py's Contested/Undetermined verdicts now always append a plain-language "why not Confirmed" clause naming the first failed test (in timing/locality/dose/control order) for every still-contending driver, built entirely from TestResult.detail — already deterministic prose challenge.py computed at S5, never new model output. Appended in narrate() after the guardrail runs, not offered to the model as a token — proven by test that the clause lands even when the model's own text already passes the guardrail cleanly (test_the_limiting_test_clause_is_appended_even_when_the_models_own_text_passes), not just on the fallback path. Scoped to Contested/Undetermined only, per the plan's own text — a Likely verdict already carries a primary driver and share, and adding this there would be redundant. Real scenario B fixture used for the Undetermined case (competitor_offer's real "no determinable onset" timing result); Contested has no committed real fixture yet (scenario G was never generated, per the standing gap already on record), so that test builds a synthetic two-contender Case directly, matching test_adjudicate.py's own synthetic-matrix convention. Mutation-tested: flipping the contender-eligibility filter (status != "eliminated" to == "eliminated") was caught by all three new tests failing. — Jitam
2026-09-02  ADA-5 (docs/ada-integration-plan.md): the UI's WhereItCameFrom block already received `tree.hhi` in every case's JSON but never rendered it — added a second sentence alongside the existing K(2) line, "N accounts carry as much risk as M equally sized ones" (1/hhi), the same inverse-Herfindahl framing ADA's own effective_segments() uses. Pure frontend addition — hhi was already flowing through types.ts and the Case JSON, so no backend/schema change was needed at all, unlike the plan's original assumption that a new field would have to be added. New tests/WhereItCameFrom.test.tsx (5 tests, the block's first dedicated test file); mutation-tested (swapping 1/hhi for hhi was caught immediately). npm run build (tsc + vite) clean. — Jitam
2026-09-02  ADA-6 (docs/ada-integration-plan.md): audited all four probes/*.sql files for DuckDB-specific syntax before repeating "Snowflake is a connector swap" anywhere official. Real findings: three of the four (incident_scan, lost_reason_scan, ticket_spike) lean on DuckDB's own `SELECT UNNEST($array_param)` idiom to expand a list-valued parameter inside an IN clause — Snowflake's equivalent (LATERAL FLATTEN) is materially different SQL, not a one-line swap; incident_scan.sql also uses a comma-lateral `unnest(string_split(...)) AS u(col)` join and DuckDB's own `string_split()` function name; lost_reason_scan.sql uses `count(*) FILTER (WHERE ...)`, ANSI-standard but not supported by Snowflake specifically. price_delta.sql alone is fully portable. Given the load-bearing UNNEST pattern appears in 3 of 4 files and a real cross-engine rewrite cannot be tested against an actual Snowflake instance from here, chose to correct the doc claim (§18) rather than attempt a speculative, untested rewrite — docs/02-architecture.md now names the actual cost (a probe-dialect rewrite alongside the connector swap) instead of asserting a swap alone. — Jitam
2026-09-02  Continuous operation, pieces 1-3 of 4 (docs/continuous-operation-plan.md), CO-1 through CO-4: src/casefile/scan.py (new — latest_closed_period, regions, scan_slice, scan, run_scheduled, main) and src/casefile/casestore.py (new — a separate data/casestore.duckdb, never data/casefile.duckdb, since loader.build() unconditionally rebuilds that file on every make data) give run_case() the "which slice, when" half orchestrator.py's own _main() docstring already admitted was missing. scan_slice() stays a thin run_case() wrapper rather than a second verify() call — run_case() already returns immediately with zero model calls when verify() fails, so verifying twice would double-pay its ~80-100 SQL-round-trip cost for nothing; both this and the case-store file-location choice were caught by a Plan-validation pass against the real code before any of this was written, not found as bugs afterward. run_scheduled() is real, tested, in-process code ("a Python function on a timer," injectable sleep, an iteration cap) — proven with iterations=3 and scan() itself replaced via monkeypatch, zero real wait and zero real run_case() cost. tools/replay_scan.py + two new Makefile targets (scan, replay) are the demo-facing proof: make replay walks the corpus's own real 2026-02/03/04 against a fresh generated corpus and prints a §11-shaped transcript; make scan runs against the committed warehouse and populates data/casestore.duckdb. All six contracts per scan, not narrowed to net_revenue first, and real run_scheduled() code rather than documentation-only, were both open questions put to the user directly and confirmed before writing any code. 571 tests pass (up from 552), ruff/mypy/both tools/ checks all clean. — Jitam
2026-09-02  Continuous operation — a real, previously-unexercised gap scan() found: 7 of 24 real slices (all six contracts x four regions, each contract at its own latest_closed_period()) raise inside verify() itself rather than returning a VerificationResult. Every existing test in this repository hand-picks a known-good contract/period/region; scan() is the first thing here that sweeps every contract against every region blindly, and measured against the real committed warehouse it hits two distinct pre-existing Stage 1 bugs neither of which any prior test triggered: _movement()'s own VerifyError ("no previous period to compare against") for a ratio/ARR-flow KPI with nothing due in one of the two compared periods for a region (gross_renewal_rate/APAC, new_business_arr/APAC, nrr/APAC), and a bare ValueError from stats/stl.py ("STL needs two full cycles") for a region whose own value series has fewer non-null observations than verify.py's _history_is_sparse() assumes — that check reads calendar span from contract.history_start, not the count of actually-defined values, so a region with real gaps (no renewals due some months) can have a long enough span but still too few observations for STL's two-cycle requirement (gross_renewal_rate and nrr, North and West both). Deliberately not fixed here: touching verify.py's sparse-history detection risks shifting the materiality false-alarm calibration already measured and committed in docs/06-quality.md §35.6 (ADA-2), and it is out of scope for a scan/casestore/scheduler task. scan() is instead made resilient to it — a slice that cannot even be verified is counted in the new ScanSummary.slices_unverifiable field and the sweep continues, the same "closing early is a success path" principle run_case() already applies one level up, extended one step further to "not opening at all is also not a crash." Caught and reverified by mutation test: catching TypeError instead of ValueError around scan_slice() was confirmed to crash the sweep, caught by test_scan_sweeps_every_contract_without_crashing_on_an_unverifiable_slice, then reverted. — Jitam
2026-09-02  Continuous operation — one more real gap, found by the mutation-testing pass rather than assumed clean: casestore.py's flat kpi/period/dimensions/priority/confidence/provisional columns (design decision 9's own reasoning for why verification_passed is its own column applies equally to the rest of the row) were written by save() but never read back by anything — load()/list_cases() only ever deserialise payload, plus verification_passed for open_only. A deliberate mutation inverting the stored provisional value passed every existing test in tests/test_casestore.py and tests/test_scan.py clean, proving the gap was real, not hypothetical. Closed by a new test (test_the_flat_columns_match_the_cases_own_fields) that queries the flat columns directly by SQL and checks each against the case's own fields; the same mutation is now caught. Also worth recording so a future mutation pass on this file is not misled by it: a rapid cp/sed/pytest/revert cycle within the same wall-clock second produced a spurious, non-reproducible test failure twice during this work, traced to CPython's own pyc cache storing source mtime at one-second resolution (PEP 552) — a restored-original file can share a cached bytecode's recorded second with the mutated version that produced it. Not a code bug; clearing __pycache__ between each mutate/test/revert step going forward avoids it. — Jitam
2026-09-02  Continuous operation, piece 4 (docs/continuous-operation-plan.md, CO-5 through CO-7): simulated incremental ingestion, chosen over the two other options put to the user (a pluggable seam only; docs only) because it's the one that's actually testable end to end here. src/casefile/data/ingest.py (new) never reconstructs scm.World() or calls generator.generate()'s _tables() pipeline — both share one continuous RNG stream across their whole run (World.rng inside _build_accounts/_propagate_tickets, and _tables()'s own rng), and re-entering either with bumped date constants would very likely reproduce the exact bug class already logged once here (a shared stream silently un-churning NORTHWIND). Instead it operates purely on the raw CSVs already on disk, appending via the stdlib csv module, with every new draw independently keyed by (seed, "ingest", period, account_id, ...) — the same World.stream() idiom, never a shared stream. Scope is deliberately split per source: billing (net_revenue) gets a real steady-state invoice continuation (recurring charges flat, usage charges lightly noised); crm (gross_renewal_rate/expansion_arr/new_business_arr/nrr) gets a trivial re-sync of account.csv's own _synced_at column, since _watermarks() only reads that column for this source — zero new rows, zero RNG risk, and it's the same thing _accounts()'s own docstring already says happens for real; product_ops (p1_resolution_time) is explicitly deferred — ticket arrival is a discrete process, not a continuation, and a safe version would touch _propagate_tickets, the one part of scm.py already flagged as shared-stream-fragile. Measured against the real corpus, not assumed: after one ingest_batch() + rebuild, net_revenue and the four crm-sourced contracts' latest_closed_period() genuinely advance to the new month while p1_resolution_time stays at 2026-04. loader.build() gained one small additive parameter (as_of: datetime | None = None, threaded into _watermarks()) so meta.watermark.as_of can advance too — every existing caller passes nothing and is unaffected, proven by every pre-existing test in test_loader.py staying green. tools/replay_scan.py now ingests and scans two new periods after its existing three-period frozen walk; Makefile's new ingest target deliberately does not depend on data (unlike scan/replay/demo) since ingest is meant to be re-run repeatedly and depending on data would reset raw/ before every call, undoing the chaining — verified by running make ingest twice in a row and getting 2026-05 then 2026-06, not 2026-05 twice. 581 tests pass (up from 571), ruff/mypy/both tools/ checks all clean. — Jitam
2026-09-02  Continuous operation, piece 4 — two real gaps found by mutation-testing rather than assumed clean, both in tests/test_ingest.py. First: an RNG key missing account_id (e.g. f"{seed}:ingest:{period}" alone) is invisible to both a plain determinism check (still deterministic run-to-run under the same seed) and a "does dropping an account shift another's output" check (each _continue_line() call builds a fresh random.Random() rather than advancing one shared object across a loop, so removing an account never shifts anyone else's stream position) — it just silently gives every account the identical noise multiplier, a third, different symptom neither existing test was built to catch. Closed with a dedicated test comparing the actual price-noise ratio applied across different real accounts. Second, found while building the first fix: that new test's own first version rounded ratios to 6 decimal places and still saw "variation" under the buggy shared key — a false negative, traced to unit_new's own 2dp rounding producing a small ratio spread purely from different accounts' unit prices even when the underlying random draw is identical (observed spread: ~0.00001 around one shared value). Rounding to 3dp instead collapses that spread back to one bucket for the buggy case while still registering real independent noise's ~4% spread as several distinct values — both the original mutation and the test's own false-negative were found and fixed before either was counted done. — Jitam
